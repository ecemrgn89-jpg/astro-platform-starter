---
const title = "BiBak | Resmi Web Sitesi";
---

<section style="
  max-width:900px;margin:0 auto;padding:40px 16px;text-align:left">
  <h1 style="font-size:clamp(28px,6vw,44px);line-height:1.1;margin:0 0 12px;color:#111">
    BiBak Test & Eğlence Merkezi
  </h1>
  <p style="max-width:46ch;font-size:clamp(15px,2.8vw,18px);color:#3D3D3D;margin:0 0 16px">
    Eğlenceli, düşündürücü ve paylaşması kolay testler. Her gün yeni bir merak!
  </p>
  <a href="/tests" style="
     display:inline-block;padding:12px 16px;border-radius:10px;
     background:linear-gradient(90deg,#ff6a00,#ff2d20);color:#111;
     font-weight:700;text-decoration:none">Testleri keşfet</a>
</section>
<style is:global>
  :root{
    --bibak-bg1:#FFFDF9;
    --bibak-bg2:#FFF5F0;
    --bibak-text:#111111;
    --bibak-sub:#3D3D3D;
    --bibak-grad1:#ff6a00;  /* turuncu */
    --bibak-grad2:#ff2d20;  /* kırmızı */
  }

  html, body {
    margin:0; padding:0;
    font-family: system-ui, -apple-system, "Segoe UI", Roboto, Arial, sans-serif;
    color: var(--bibak-text);
    background: linear-gradient(180deg, var(--bibak-bg1) 0%, var(--bibak-bg2) 100%);
  }

  /* Link’ler */
  a { color: var(--bibak-text); text-decoration: none; }
  a:hover { opacity:.9; }

  /* Başlık/p paragraf küçük ayar */
  h1{ letter-spacing:-0.3px; }
  p { color: var(--bibak-sub); line-height:1.6; }

  /* CTA butonları tek yerde kontrol edelim */
  .cta{
    display:inline-block; padding:12px 16px; border-radius:10px;
    background: linear-gradient(90deg, var(--bibak-grad1), var(--bibak-grad2));
    color:#111; font-weight:700; text-decoration:none;
    box-shadow: 0 6px 22px rgba(0,0,0,.06);
    transition: transform .06s ease;
  }
  .cta:active{ transform: translateY(1px); }
</style>
