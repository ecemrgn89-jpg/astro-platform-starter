---
import Layout from "../layouts/Layout.astro";
---

<Layout title="BiBak">
  <header class="navbar">
    <nav>
      <a href="#">Ana Sayfa</a>
    </nav>
  </header>

  <section class="hero">
    <div class="container">
      <h1>BiBak’a Hoş Geldiniz</h1>
      <p class="intro">
        BiBak; gündemi, kültürü ve yaşamı güvenilir bir dille anlatan, okuruna saygılı bir yayın.
      </p>

      <h2 class="sub">Testleri Keşfet 🎯</h2>
      <p class="desc">
        Eğlenceli, düşündürücü ve kendini tanımaya yardımcı testler seni bekliyor.
      </p>
      <a href="#" class="btn">Testleri gör</a>

      <h2>Ne Sunuyoruz?</h2>
      <ul>
        <li>Özgün içerikler ve seçkiler</li>
        <li>Hızlı, sorunsuz ve mobil uyumlu deneyim</li>
        <li>Topluluk özellikleri (yorumlar, listeler vb.)</li>
      </ul>

      <h2>Neden BiBak?</h2>
      <ul>
        <li>Şeffaf editör politikası ve doğruluk prensibi</li>
      </ul>
    </div>
  </section>

  <style>
    body {
      margin: 0;
      font-family: system-ui, sans-serif;
      background: linear-gradient(180deg, #fff9f5 0%, #fff5ee 40%, #fffaf7 100%);
      color: #222;
    }

    .navbar {
      background: #fffdfa;
      border-bottom: 1px solid #f1eae4;
      padding: 1rem 1.5rem;
      text-align: center;
      position: sticky;
      top: 0;
      z-index: 10;
    }

    nav a {
      text-decoration: none;
      font-weight: 600;
      color: #111;
      font-size: 1.1rem;
    }

    .hero {
      padding: 5rem 1.5rem;
      text-align: left;
      min-height: 100vh;
    }

    .container {
      max-width: 700px;
      margin: 0 auto;
    }

    h1 {
      font-size: clamp(2.6rem, 6vw, 3.6rem);
      font-weight: 800;
      margin-bottom: 1rem;
      color: #1b1b1b;
    }

    h2 {
      font-size: clamp(1.8rem, 4vw, 2.4rem);
      font-weight: 700;
      margin-top: 3rem;
      margin-bottom: 1rem;
      color: #1b1b1b;
    }

    .sub {
      font-size: clamp(2rem, 5vw, 3rem);
      font-weight: 800;
      margin-top: 3rem;
    }

    p,
    li {
      font-size: 1.1rem;
      line-height: 1.6;
      color: #333;
    }

    .intro {
      margin-bottom: 1.5rem;
      max-width: 600px;
    }

    .desc {
      margin-bottom: 1rem;
    }

    .btn {
      display: inline-block;
      margin-top: 1rem;
      background: linear-gradient(to right, #ff7b39, #ff4f5a);
      color: white;
      padding: 0.75rem 1.5rem;
      border-radius: 8px;
      text-decoration: none;
      font-weight: 600;
      box-shadow: 0 4px 10px rgba(255, 123, 57, 0.3);
      transition: transform 0.2s ease, opacity 0.2s ease;
    }

    .btn:hover {
      transform: translateY(-2px);
      opacity: 0.9;
    }

    ul {
      list-style-type: disc;
      padding-left: 1.5rem;
    }
  </style>
</Layout>